import 'package:flutter/material.dart';

class CommonStyle {
  static const separated = 15.0;
  static const basePadding = EdgeInsets.all(20);
  static const basePaddingX = EdgeInsets.symmetric(horizontal: 20);
  static const baseMarginY = EdgeInsets.symmetric(vertical: 20);
}
